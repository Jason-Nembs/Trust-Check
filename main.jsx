import React from "react";
import ReactDOM from "react-dom/client";
import TrustScoreApp from "./TrustScoreApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrustScoreApp />
  </React.StrictMode>
);
